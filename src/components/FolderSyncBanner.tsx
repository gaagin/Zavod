import React, { useEffect, useState } from 'react';
import { useFactory } from '../context/FactoryContext';
import { 
  FolderCheck, 
  RefreshCw, 
  Zap, 
  X, 
  FileJson, 
  CheckCircle2, 
  ArrowRight,
  HardDrive
} from 'lucide-react';

export const FolderSyncBanner: React.FC = () => {
  // Suppressed per user request: sync occurs silently in the background
  return null;
};

